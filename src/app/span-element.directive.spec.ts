import { SpanElementDirective } from './span-element.directive';

describe('SpanElementDirective', () => {
  it('should create an instance', () => {
    const directive = new SpanElementDirective();
    expect(directive).toBeTruthy();
  });
});
